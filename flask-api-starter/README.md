# flask-api-starter

A minimal Flask API boilerplate with health-check and notification endpoints,
backed by an S3 upload bucket and a Slack alert integration.

## Running

```
pip install -r requirements.txt
python app.py
```
